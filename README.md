# How to setup the project

### Step 1

`npm i`

### Step 2

go to /supabase and run the command
`npx supabase start`
This will run docker container with the proper ports for supabase

### Step 3

`npx prisma generate && npx prisma migrate dev`

### Step 4

`npm run dev`
